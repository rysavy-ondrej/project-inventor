# Python dependencies for src/security (TLS, SSH and LDAP probes).
# Versions are aligned with deploy/inventor-requirements.txt.

pyOpenSSL==24.1.0
scapy==2.5.0
ldap3==2.9.1
icmplib==3.0.4
pytest~=8.2.2
