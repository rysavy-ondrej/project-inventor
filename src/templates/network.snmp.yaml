monitors:
  - name: network.snmp
    module: network_snmp
    exec: src/network/network.snmp

schedule:
  - test: network.snmp
    write-to: network.snmp
    targets:
        - { "target_host": "www.example.com", "oids": "1.3.6.1.2.1.1.5.0", "community_string": "public" }
    repeat-every: 5m
